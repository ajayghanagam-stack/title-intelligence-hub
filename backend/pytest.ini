[pytest]
testpaths = tests benchmarks
addopts = -m "not llm_eval"
markers =
    benchmark: Mocked benchmark tests (CI-safe, no API calls)
    benchmark_live: Live benchmark tests (requires GOOGLE_API_KEY)
    llm_eval: LLM evaluation tests (requires GOOGLE_API_KEY and golden datasets)
asyncio_mode = auto
